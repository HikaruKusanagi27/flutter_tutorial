import 'package:flutter/material.dart';

class RiverpodTutorialPage extends StatelessWidget {
  const RiverpodTutorialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Riverpod Tutorial')));
  }
}
